import { Page } from '@playwright/test';\n\nexport class LoginPage {\n  private page: Page;\n  private loginBtn = this.page.locator(\"//a[.='Log in']\");\n  private usernameTxt = this.page.locator(\"//input[@id='loginusername']\");\n  private passwordTxt = this.page.locator(\"//input[@id='loginpassword']\");\n  private signInBtn = this.page.locator(\"//button[.='Log in']\");\n  private welcomeMsg = this.page.locator(\"//a[.='Welcome PeterPanter']\");\n\n  constructor(page: Page) {\n    this.page = page;\n  }\n\n  async clickLogin()